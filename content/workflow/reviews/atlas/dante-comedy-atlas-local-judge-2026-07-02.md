# Lens Atlas: Dante's Comedy Local Judge - 2026-07-02

Target: `website/src/content/docs/lens.md`

Change reviewed: add a compact `Lens Atlas: Dante's Comedy` surface to the public atlas, using the existing `Dante As Poetic Access And Reception` page as the hub rather than creating a new global Dante page or sidebar cluster.

Mode: local reader/world-model judge plus grounding/provenance judge. Independent judge agents were not used.

## Reader / World-Model Judge

Finding: PASS with residual risk.

The atlas edit gives readers a public map entry for the recent Dante lecture cluster without duplicating the existing Dante hub. It names the smallest public surface: keep the cluster lens-side, point to the existing access/reception page, and use the atlas only to explain why Inferno, Purgatorio, Paradiso, Virgil, virtues, love, fraud, and competition should route by active mechanism rather than become a general Dante overview.

Residual risk: the source list is dense. Future Dante additions should not extend this atlas section source by source unless they change the atlas boundary. New material should deepen an existing page, promote a reviewed child only if a mechanism matures, or replace/compress this routing note.

## Grounding / Provenance Judge

Finding: PASS with residual risk.

The intro claim is source-backed by the April 2026 Dante access/reception spans, and the routing paragraph uses the Purgatory/Virgil source for the concrete guide-trap boundary. The lecture list uses exact public episode links rather than vague references. The edit does not move lens-point IDs and does not alter episode read JSON.

Residual risk: the atlas section links many sources but marks only the claims that carry interpretive pressure. This is acceptable for an atlas surface because the detailed source trails now live on the existing Dante hub and neighboring concept pages. Future work should add evidence marks only when the atlas itself makes a new source-backed boundary claim.

## Boundary Decision

Accepted locally: the smallest public change is an atlas section plus existing hub, not a new standalone Dante concept page and not a sidebar cluster yet. If a sidebar cluster is later added, use Socrates' requested label `Lens Atlas: Dante's Comedy`, but this mutation does not need sidebar curation because the existing page already carries the detailed cluster map.
